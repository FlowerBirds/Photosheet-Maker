{
  "name": "photosheet-maker",
  "version": "0.1.0",
  "description": "Browser-only photo sheet layout tool",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "canvas": "^3.2.3",
    "jsdom": "^29.1.1",
    "vitest": "^2.0.0"
  }
}
